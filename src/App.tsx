export function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>Drone Derby v2 — engine phase</h1>
      <p>
        The pure game engine lives in <code>src/engine/</code>. UI arrives in
        Phase 2 (hot-seat + replay player).
      </p>
    </main>
  );
}
