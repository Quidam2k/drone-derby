# Cascade: Drone Derby v2 — From-Scratch Rewrite

**Created:** 2026-07-05
**Status:** Phases 1–4 DONE — LIVE at https://drone-derby.pages.dev (prod push round-trip verified). Todd still owes: fun-playtest verdict + phone/iOS installed-PWA push check.

## Vision

Async multiplayer RoboRally-style programming game, browser-first PWA (no app
stores). Core loop: get a push notification → watch the last turn replay with
playback controls → program your 5 registers → submit → when the last player
submits, the turn executes and everyone gets notified. Games span days to a
week+.

## Cross-Phase Decisions (locked unless revisited)

- **Backend: Convex** (DB + auth + reactive queries + serverless functions).
  Replaces the entire old Express/Postgres/Redis/Socket.io stack. Pivot to
  Supabase possible later; keep Convex-specific code confined to `convex/`
  and thin client hooks.
- **Engine is a pure function**: `(gameState, programs, seed) → (newState,
  EventLog)`. Zero dependencies, deterministic (seeded RNG), lives in
  `src/engine/`, imported by BOTH the client (hot-seat, previews, replay) and
  Convex functions (authoritative execution). Never touches DB/DOM.
- **EventLog is the animation contract**: turn execution emits atomic events
  (move, rotate, push, conveyor, laser, damage, fall, respawn, checkpoint…)
  grouped by register. The replay player consumes only the EventLog. Stored
  per turn in Convex so any past turn is replayable.
- **Rendering: DOM/CSS grid** for the game board (tiles + transitions), NOT
  canvas. (Amended 2026-07-06, Todd: the level editor is ALSO DOM/CSS grid,
  reusing the game's Board/Tile renderer — WYSIWYG, zero new deps. Canvas is
  off the table unless a future feature demands it.)
- **State: Zustand** for client state. No Redux. No MUI — plain CSS
  (custom properties, grid/flex); can add Tailwind later if wanted.
- **Auth: Convex Auth** (magic link + Google OAuth). No passwords, ever.
- **One app, one package.json**: Vite + React + TS at repo root, `convex/` for
  backend functions. No monorepo/workspaces.
- **Old codebase archived** at `legacy/` — reference only (card/tile ideas,
  editor UX). Never import from it.
- **Rules scope (MVP)**: RoboRally-lite. Cards: Move 1/2/3, Back Up, Rotate
  L/R, U-Turn, priority 10–840. Tiles: floor, walls (edge-based), pits,
  conveyors (normal + express), gears, checkpoints, spawn docks, board
  lasers. Robots fire lasers. Damage locks registers (5+) and kills at 10;
  3 lives; respawn at last checkpoint. Cut for MVP: pushers, repair sites,
  option cards, power-down.

## Phases

### Phase 1 — Archive + Scaffold + Core Engine  [DONE 2026-07-05]
Delivered: legacy archived to `legacy/`; git repo initialized (root commit
eb190f2); Vite 6 + React 19 + TS strict + Vitest scaffold at root; pure
engine in `src/engine/` (types, board, rng, deck, setup, execute, events,
boards, index) — 54 tests green incl. determinism + golden EventLog
snapshot. CLAUDE.md rewritten; `docs/game_mechanics_md.md` updated to the
confirmed full-lite scope. Notes for later phases:
- ~~Vite pinned to ^6 because Node here is 20.18.0 (Vite 7 wants 20.19+).~~
  Resolved 2026-07-13 (§16): Node 22.23.1 via nvm; vite ^7.3.6,
  vitest ^4.1.10, @vitejs/plugin-react ^5.2.0.
- Engine API: `createGame(board, playerIds, seed)`,
  `executeTurn(state, programs, seed)`, `Program = (Card|null)[]` (null =
  idle slot, useful for AFK auto-submit later). `provingGrounds()` is the
  built-in test board. Respawn facing is fixed N (chooser deferred).

### Phase 2 — Hot-Seat Game + Replay Player  [DONE 2026-07-05]
Delivered: Zustand store (`src/store/gameStore.ts`) — screen state machine
setup→handoff→programming→replay→gameover; turn seed = `initialSeed +
game.turn` so whole games replay from one number. DOM/CSS-grid board
renderer (`src/components/board/`) driven by a `VisualState` (never
GameState) so live view and replay share it. EventLog replay player
(`src/components/replay/`): pure `applyEvent`/`visualAt` reducer +
play/pause/step-back/fwd/skip/0.5–4× controls, register indicator, event
captions, laser-beam + bump overlays. Programming UI
(`src/components/programming/`): hand → 5 registers (locked registers
shown with held card), handoff pass-the-device gate, setup + game-over
screens. Plain CSS dark theme in `src/index.css`. 58 tests green (4 new
reducer tests incl. full-game lockstep vs the engine on Proving Grounds).
Browser-verified via Playwright MCP (screenshots in `screengrab/`).
Notes for later phases:
- `robot-respawned` implies a damage reset (engine emits no damage event
  on respawn) — the visual reducer sets `RESPAWN_DAMAGE`; keep in sync.
- Robot facing animates via cumulative angles (shortest-path CSS spin).
- **Todd's fun-playtest still pending** — do it before sinking time into
  Phase 3; rules/pacing tweaks are cheapest now.

### Phase 3 — Convex Multiplayer  [DONE 2026-07-06]
Delivered: Convex backend (`convex/`) — schema (games, players, turns,
submissions + auth tables), Convex Auth with **Anonymous provider** ("Play
as guest"), games.ts mutations createGame/joinGame/startGame/submitProgram/
markTurnSeen + queries myGames/gameByInvite/game/turn. Last active submit
runs `executeTurn` from `../src/engine` server-side (seed = game.seed +
turn) and stores `{prevState, events}` per turn. Hands are secret at the
wire level: `sanitizeState` strips decks entirely and all hands but the
caller's (verified via authed HTTP-API probe). Client: hash routing
(`src/services/route.ts`: #/ lobby, #/hotseat, #/game/<id>, #/join/<code>),
lobby/join/online-game screens (`src/components/online/`), hot-seat moved to
`src/components/hotseat/HotSeatGame.tsx` (untouched store, offline path).
Unseen turns auto-replay oldest-first through the existing ReplayPlayer,
then `markTurnSeen`. 58 tests + typecheck green; two-browser-context
Playwright E2E: create → invite-join → both program → identical turn-1
replay end positions in both contexts, turn-2 loop, lobby "your move!"
status (screenshots `screengrab/phase3-*.png`).
Notes for later phases:
- **Local dev runs with NO Convex account**: `CONVEX_AGENT_MODE=anonymous
  npx convex dev` (anonymous local deployment, data under ~/.convex;
  `.env.local` points VITE_CONVEX_URL at it). Phase 4 deploy needs
  `npx convex login` + project creation, then re-set env vars below.
- Convex Auth env vars set on the deployment: JWT_PRIVATE_KEY (PKCS8 PEM,
  **single line, newlines→spaces** — multiline values get mangled through
  the Windows CLI), JWKS, SITE_URL. Regenerate for prod (keys in scratchpad
  are throwaway; use `npx @convex-dev/auth` or jose).
- Magic link (Resend) + Google OAuth deferred pending Todd's credentials:
  add providers in `convex/auth.ts` + UI in `SignInGate` (common.tsx);
  nothing downstream cares which provider made the userId.
- Player display name doubles as engine PlayerId (unique per game,
  enforced at join). `games.currentTurn` = turn being programmed;
  executed turns = 1..currentTurn-1; `players.lastSeenTurn` drives replay.
- `structuredClone` confirmed available in the Convex runtime (engine runs
  unmodified). `convex/_generated` is committed so typecheck works fresh.

### Phase 4 — PWA + Push Notifications + Deploy  [DONE 2026-07-06]
DONE (verified locally, 2026-07-06):
- PWA shell: vite-plugin-pwa **injectManifest** (custom `src/sw.ts` carries the
  push handlers generateSW can't); manifest + robot icon set generated by
  `npm run icons` (scripts/generate-icons.mjs, sharp); iOS meta in index.html.
  `src/sw.ts` typechecks via separate `tsconfig.sw.json` (WebWorker lib
  conflicts with DOM — excluded from the app tsconfig).
- Web push: `pushSubscriptions` table (dedupe/upsert by endpoint);
  `convex/notifications.ts` (subscribe/unsubscribe/vapidPublicKey + internal
  forUsers/prune); `convex/push.ts` `"use node"` action using web-push, no-ops
  until VAPID env vars exist, prunes dead subs on 404/410. Scheduled via
  `notifyOthers()` in games.ts from `startGame` + `submitProgram`
  post-execution (tag = game id so notifications collapse). Client:
  `src/services/push.ts` + NotificationsButton (lobby header, game lobby,
  waiting view). SW tap → focus + postMessage navigate (listener in push.ts).
- Verified: typecheck + 58 tests green; prod build; SW activates on
  `npm run preview`; **full local round-trip** — subscribe in headless Chrome →
  `npx convex run push:send` → FCM → notification shown with right payload
  (screengrab/phase4-lobby-notifications-on.png). Local VAPID keys are set on
  the anonymous deployment (throwaway — generate fresh for prod).
DEPLOYED (2026-07-06):
- GitHub: https://github.com/Quidam2k/drone-derby (public, branch master).
- Convex project `drone-derby`, team `toddcwalker`. Dev deployment
  vivid-cat-177 (now in .env.local — cloud dev, replaces anonymous local);
  prod fastidious-dinosaur-923. Both have JWT_PRIVATE_KEY/JWKS (separate
  keypairs), SITE_URL, VAPID_* set. Dashboard:
  https://dashboard.convex.dev/t/toddcwalker/drone-derby
- Frontend: Cloudflare Pages project `drone-derby` (account
  toddcwalker@gmail.com) → https://drone-derby.pages.dev. Redeploy anytime
  with `npm run deploy` (convex deploy --yes bakes prod VITE_CONVEX_URL into
  the build, then wrangler pages deploy; both CLIs logged in on this PC).
- Prod smoke PASSED: SW activated, manifest valid, guest sign-in (JWT keys
  good), push subscribe → `npx convex run push:send ... --prod` → notification
  received in browser (screengrab/phase4-prod-lobby.png).
- CLI gotchas learned: `convex env set -- NAME value` (leading `-----` in PEM
  parses as a flag without `--`); `convex deploy --yes` and
  `convex login --no-open --login-flow poll` for non-interactive shells.
REMAINING (Todd, non-blocking): fun-playtest verdict; install PWA on phone at
the prod URL, enable notifications, verify a push lands (iOS 16.4+ requires
the installed app). Rules/pacing tweaks fold into Phase 5.

### Phase 5 — Async Quality-of-Life + Polish  [DONE 2026-07-06 (code + dev E2E)]
**Scope decision (Todd, 2026-07-06): NO turn deadlines, NO AFK enforcement.**
Play-when-you-can, like Words with Friends — deadlines deferred indefinitely
(may return later as a per-game *option*). Game is among friends, so social
pressure (nudges) replaces enforcement.
Delivered:
- **Nudge** (`games.nudge` mutation): any member not being waited on pokes
  the waiting players via the existing push pipeline; lobby variant (host →
  everyone else). Rate limit: one per game per 12h (`games.lastNudgeAt`;
  `nudgeAvailableAt` returned by the `game` query). UI: 👉 button in
  WaitingView's waiting note, disabled with next-available tooltip during
  cooldown. Server rejects bypass attempts with a clean error (verified via
  authed HTTP-API probe).
- **Speech bubbles** (Todd's 2026-07-06 request): optional ≤60-char line
  submitted with a program (`taunt-input` in ProgrammingView, both modes).
  Stored on submissions, folded into the turns row at execution
  (`turns.taunts` by player name), returned by `games.turn`. Rendered by
  ReplayPlayer: bubble pops at the player's first card-revealed event and
  lingers 8 events (`src/components/replay/taunts.ts`, unit-tested). Rides
  ALONGSIDE the EventLog — engine/event union untouched. Hot-seat threads
  taunts through the store (`pendingTaunts`).
- **History browser** (`src/components/online/HistoryBrowser.tsx`): "Watch
  past turns" from WaitingView + game-over screen; replays any executed turn
  via `games.turn` and NEVER calls markTurnSeen. Rendered before the
  unseen-turn catch-up branch so an in-progress browse isn't yanked.
- **Stats**: GameOverScreen per-player summary (checkpoints/lives from the
  final GameState, shared with hot-seat); lobby rows show `turn N` chip +
  per-player ✓/⏳ badges (myGames data, no new storage).
- Verified: typecheck + 61 tests green (3 new taunt-window tests); prod
  build; two-browser-context Playwright E2E on the dev deployment — nudge
  send + reactive cooldown + server-side rejection, bubbles on both screens
  turn 1, history list/replay/return with turn-3 programming state intact,
  lobby badges (screengrab/phase5-*.png).
Deferred to playtest verdict (gate still open): rules/pacing tweaks, mobile
layout pass, SVG sprites, second board. Optional Resend/Google auth still
waiting on creds.

### Phase 6a — Level Editor Core (local-first)  [DONE 2026-07-06]
**Decisions (Todd, 2026-07-06): DOM-grid editor (not canvas), split 6a
(editor core, local drafts, hot-seat test-drive) / 6b (Convex persistence +
board picker).** Legacy v1 editor mined for UX only (three-panel layout,
live validation, right-click erase, drag-paint); its gaps became
requirements (undo/redo, resize, edge walls, working import/export).
Delivered:
- **`src/engine/validate.ts`** — pure `validateBoard(board)` →
  `{errors, warnings}`; errors: size 6–16, full grid, 2–4 spawns + ≥1
  checkpoint numbered 1..n contiguous, walls/lasers in bounds, no emitter
  on a pit. Warnings: bland board (no hazards), express belt not part of a
  belt line, spawn in a laser line. Reused server-side in 6b.
- **`src/store/editorStore.ts`** — draft board + tools (conveyor dir/express,
  gear spin; wall/laser place on cell EDGES, laser fires away from its
  mounted edge), snapshot undo/redo (cap 50, drag = one stroke = one step),
  auto-numbering (lowest free number; erasing renumbers nothing — validation
  flags gaps), resize preserving overlap, debounced localStorage draft
  (`droneDerby.editorDraft`), validation recomputed per change.
- **`src/components/editor/`** — EditorScreen at `#/editor` (works without
  Convex; lobby card links to it): toolbar (name, W/H steppers, undo/redo +
  Ctrl+Z/Y, export/import JSON with validateBoard gate, clear, test drive),
  ToolPalette, EditorBoard (real Board renderer + transparent hit layer,
  4 edge zones per cell, right-click erases), ValidationPanel.
- **Test drive**: `gameStore.startGame(names, board?)` optional board;
  editor button (disabled on errors) starts a 2-pilot hot-seat game on the
  draft; hot-seat "new game" falls back to provingGrounds.
- Verified: typecheck + 85 tests (24 new: validate + editorStore); Playwright
  E2E — paint/drag/erase, wall+laser edges, undo/redo/Ctrl+Z, resize (drops
  OOB, validation catches lost spawns), reload draft survival, export JSON,
  import gate (rejects bad file, loads good one, single undo step),
  test-drive full turn on a 12×9 custom board. screengrab/phase6a-editor.png.
- Engine untouched except new validate.ts + index.ts export; EventLog as-is.

### Phase 6b — Boards in Convex + board picker  [DONE 2026-07-06, deployed]
Delivered:
- **`boards` table** `{name, createdBy, board(v.any BoardDef), updatedAt}` +
  `by_creator` index. **`convex/boards.ts`**: `save` (insert or creator-only
  patch; trims/caps name 1–40; server-side `validateBoard` gate — throws
  first error; 50 boards/user cap), `myBoards` (light summaries, updatedAt
  desc), `get` (full BoardDef, creator-only, null if foreign/missing),
  `remove`. `requireUserId` lifted to shared `convex/helpers.ts`.
- **Games on custom boards**: `games.board` = optional **BoardDef snapshot
  taken at createGame** (edits/deletes of the source never touch a game);
  `createGame({boardId?})` validates + snapshots + sets boardName;
  `startGame` runs the engine on the snapshot (fallback provingGrounds).
  **Seat cap = min(4, board's spawn count)** enforced in joinGame +
  startGame + gameByInvite.full (which now also returns `seats`; JoinScreen
  shows the real max instead of hardcoded 4).
- **Lobby picker**: `<select>` in "New online game" (built-in first, then
  myBoards as "name — W×H"); hidden when the user has no saved boards.
- **Editor cloud save**: `#/editor/<boardId>` route; "Save online" button
  (Convex-configured builds only, disabled on validation errors) signs in
  anonymously if needed — **via a pendingAuth effect, since signIn()
  resolves before the client carries the token** — then navigates to the
  new id; later saves patch in place ("Saved ✓" note). CloudBoardLoader
  hydrates boards.get into the store once per boardId (module-level
  `hydratedBoardId` guard prevents the reactive query clobbering fresh
  edits after save). `#/editor/<id>` wraps in SignInGate; plain `#/editor`
  stays public/offline.
- Verified: typecheck + 85 tests; 4-context Playwright E2E on dev deploy —
  build 8×10 board (3 spawns) → save online → localStorage draft wiped →
  reload restores from cloud → in-place resave → HTTP probe: invalid board
  save rejected server-side → picker shows board → create game → Bravo +
  Charlie join → 4th join blocked in UI AND by forced HTTP joinGame ("fits
  3 players") → start → all 3 program turn 1 → replay renders the custom
  board (80 tiles/3 spawns/1 pit) on every client.
- Deployed to prod (Convex fastidious-dinosaur-923 + Cloudflare Pages);
  prod smoke: editor → save online → picker → custom-board game created.
Out of scope (still): gallery/sharing/ratings, thumbnails, mobile editor
layout, board versioning.

### Phase 7 — Mobile layout pass  [DONE 2026-07-06, deployed]
Delivered (game screens only; editor mobile explicitly out of scope):
- **Board self-sizes to the viewport**: `Board.tsx` sets `--tile` inline via
  exported `tileFit(board)` = `min(52px, (100vw - 3rem)/W, (100dvh - 12rem)/H)`.
  52px cap ⇒ desktop pixel-identical; the 3rem width reserve covers screen
  padding + board border + a desktop scrollbar. `EditorBoard` puts the same
  var on `.editor-board-wrap` so the hit layer stays aligned (verified:
  paint at cell 4,4 lands exactly). The old `--tile: 34px` media query is
  gone — boards of any size now fit any viewport width.
- **`index.css` ≤700px block** (phase-7 section at the bottom): tighter
  `.screen` padding; `.player-strip` goes horizontal/wraps under the board;
  registers 5-up in one row + hand cards 5 per row at
  `calc((100vw - 4.5rem)/5)`; programming footer wraps with the taunt input
  full-width on top (`order: -1`); replay + lobby controls get ≥44px touch
  targets; lobby header + invite box wrap.
- **Global niceties**: `touch-action: manipulation` on button/input/select/a
  (kills double-tap zoom); body padding from `env(safe-area-inset-*)`
  (installed-PWA notch); `.center-screen` 100vh → 100dvh; `.title`
  `clamp(2.1rem, 9vw, 3rem)`. `viewport-fit=cover` was already in index.html.
- Numbers at 375×667: programming page 1204px → 859px tall (board+strip on
  the first screen, registers/hand one small scroll), replay screen fits
  entirely (controls bottom 643px), game lobby/join/setup/handoff/lobby all
  overflow-free. 10-wide board = 32.7px tiles = 333px.
- Verified: typecheck + 85 tests; Playwright — full hot-seat turn at 375×667
  (tap-to-place all 5 registers × 2 seats → replay), online turn across two
  separately-authed contexts (375×667 + 390×844) with zero horizontal
  overflow anywhere; desktop 1280×800 regression (52px tiles, 92×112 slots,
  vertical strip — unchanged); editor hit-layer alignment. Deployed
  (Convex prod + Cloudflare Pages); prod spot-check at 375 matches local
  (tile 32.7px, no overflow). Screenshots: screengrab/m7-*.png.
Out of scope (still): editor mobile layout, landscape tuning, native packaging.

### Phase 8 — Second built-in board: Spin Cycle  [DONE 2026-07-06, deployed]
Delivered:
- **`spinCycle()`** in `src/engine/boards.ts`: 12×10, a clockwise conveyor
  loop (16 normal belts ringing the 6×4 center), express on-ramps feeding
  the loop from N (6,1–2) and S (5,7–8), gears on the loop's outside
  corners, pit (6,4) + checkpoint 3 (5,5) inside the ring, checkpoints
  1 (10,2) / 2 (1,7) on opposite outside corners, crossfire lasers sweeping
  rows 4 and 5 from (0,4)E and (11,5)W, 3 tactical walls, 4 spawns at y=9.
  Riding the loop is fast but crosses the laser rows; walking is safe/slow.
- **`BUILTIN_BOARDS` registry** (same file, exported via engine index):
  stable key → `{name, factory}`, entry order = picker display order. One
  list shared by client pickers and Convex.
- **`createGame({builtin?})`** in convex/games.ts: mutually exclusive with
  `boardId` (throws on both), unknown key throws, valid key **snapshots the
  BoardDef into `games.board`** like custom boards. No args = legacy
  Proving Grounds default (no snapshot) — old clients/games unaffected.
- **Pickers**: LobbyScreen `<select>` now always visible — built-ins first
  as `builtin:<key>` option values, then saved boards; create() maps prefix
  → `builtin` arg, else `boardId`. SetupScreen (hot-seat) gains a built-ins
  select feeding the existing `startGame(names, board?)` param.
- Tests 85 → 86: validate.test now loops all BUILTIN_BOARDS (name matches,
  zero errors AND warnings); determinism.test adds a 4-player Spin Cycle
  clone-and-compare smoke turn.
- Verified: typecheck + 86 tests; Playwright — hot-seat full turn + replay
  on Spin Cycle at 375px (loop/feeders/gears/pit/lasers all render, board
  self-sizes via tileFit); online 2-context game via the picker (lobby says
  "Spin Cycle", 4 seats from snapshot, server-executed turn, both replays,
  390px zero horizontal overflow, desktop 1280×800 normal); default
  Proving Grounds create still works. Deployed (Convex prod + CF Pages);
  prod smoke: picker shows both boards, Spin Cycle game created live.
  Screenshots: screengrab/spin-cycle-*.png, online-spin-cycle-*.png.
Out of scope (still): board gallery/sharing, picker thumbnails, editor
changes, SVG sprites.

### Phase 9 — SVG tile & robot sprites  [DONE 2026-07-06, deployed]
Delivered (rendering only — engine/EventLog/store untouched):
- **`src/components/board/sprites.tsx`** (new): inline-SVG components, all
  `viewBox="0 0 52 52"` scaling via `.sprite {width/height:100%}` so
  `tileFit` mobile sizing needed zero changes. PitSprite (hazard-dashed
  rim in --wall yellow + concentric hole), ConveyorSprite (belt band +
  rails, chevrons drawn N and rotated per dir; 2 grey chevrons normal /
  3 --accent chevrons express), GearSprite (8-tooth polygon from a
  module-level `gearPoints()` generator, axle hole, arc+arrowhead showing
  spin; CCW = horizontal mirror), CheckpointSprite (ring + compass ticks +
  number), SpawnSprite (dashed dock plate + corner bolts + number),
  RobotSprite (nose wedge/treads/visor+eyes in translucent black-white,
  drawn over the player-colored .robot-body div — per-seat fill unchanged),
  EmitterSprite (mount + barrel + --danger muzzle, drawn E and rotated).
- **Tile.tsx**: `tileContent()` returns sprites; `tile-<kind>` classes and
  emitter divs (positioning classes + title) kept, so editor hit layer and
  selectors untouched. **Board.tsx**: `.robot-body` renders `<RobotSprite/>`
  inside the same rotation/translation transforms. **ToolPalette**: tool
  buttons now use the real sprites as icons (+ tiny inline floor/wall SVGs).
- **index.css**: `.tile-glyph/.pit-glyph/.conveyor-glyph/.gear-glyph/
  .checkpoint-glyph/.spawn-glyph/.robot-nose` retired; `.sprite` sizing
  added; `.emitter` is now a transparent 0.32-tile container (glow via
  `filter: drop-shadow`); `.robot-body` keeps shadow/radius, loses flex.
  Replay animation CSS untouched — sprites live inside the animated
  containers.
- Verified: typecheck + 86 tests (none touched glyph internals); Playwright
  hot-seat Spin Cycle full turn + replay (rotation transform turns the
  sprite correctly, smooth-angle W→N still works), beam overlay draws above
  sprites (layering check), editor paint/hit alignment exact (pit/conveyor/
  checkpoint/wall/laser painted via hit cells land centered), online
  2-player game (fresh-guest 2nd tab via localStorage swap) renders
  identically, mobile 375px legible at ~27px tiles, desktop regression.
  Deployed (Convex prod + CF Pages); prod smoke: Spin Cycle hot-seat shows
  36 sprites on the live bundle. Screenshots: screengrab/sprites-*.png.
Out of scope (still): card-face art (ProgrammingView glyph arrows stay),
board thumbnails, animated sprite effects, editor mobile layout.

### Phase 10 — Board-picker thumbnails  [DONE 2026-07-06, deployed]
Delivered (engine/store untouched; two read-only Convex query additions):
- **`src/components/board/BoardThumb.tsx`** (new): `BoardThumb({board,
  tilePx=8, maxPx=160})` — the real `Tile` renderer in a `.board
  board-thumb` grid with inline `--tile: min(tilePx, maxPx/W)` (NOT
  viewport-based tileFit), `aria-hidden`, no robots layer. Thumbnails
  inherit sprites + future art for free. Same file: **`BoardPicker`**
  (`options: {value, name, board, badge?}[]`) — radio-card group,
  `role="radiogroup"`/`radio` + `aria-checked`, `data-testid=
  "board-picker"` kept on the wrapper, per-card
  `board-option-<value>`. `boardCellMaps(board)` extracted from Board.tsx
  (walls/emitters by "x,y") and shared by Board + BoardThumb.
- **SetupScreen**: `<select>` → `BoardPicker` over module-level
  `BOARD_OPTIONS` (factories run once at module load); start flow
  unchanged (`BUILTIN_BOARDS[key].factory()`). **LobbyScreen**: `<select>`
  → `BoardPicker`, builtins first (badge "built-in", `builtin:<key>`
  values) then saved boards (doc ids) — value scheme and create() mapping
  unchanged.
- **Convex (plan deviation — myBoards did NOT carry defs)**: `boards.
  myBoards` now returns `board: def` per row (was width/height-only
  summary; needed for saved-board thumbs). `games.game` returns
  `board: gameBoard(game)` while `status === 'lobby'`, null otherwise
  (active games carry it in state). Both additive — no existing field
  changed.
- **Nice-to-have landed**: online game lobby (waiting room) shows a
  14px-tile BoardThumb of the snapshotted board above the seat list.
  JoinScreen skipped (only knows boardName; would need a new query).
- **index.css**: `.board-picker` flex-wrap card row; `.board-option`
  column card, accent border + ring via `[aria-checked='true']`;
  `.board-thumb` 1px border + slimmed wall strips (2px, game walls are
  fixed-px and would swamp 8px tiles) + emitter glow filter off; ≤700px:
  picker goes `flex-wrap: nowrap; overflow-x: auto` with scroll-snap.
- Verified: typecheck + 86 tests; Playwright — hot-seat create from Spin
  Cycle card (handoff → programming on 12×10), online create from builtin
  card AND from a saved-board card (board "Thumb Test" made live in the
  editor + Save online), game-lobby thumb shows for both, 375px lobby
  scrolls cards horizontally / setup fits 2 cards, desktop regression.
  Deployed (Convex prod + CF Pages); prod smoke shows thumbnail picker
  (needed SW update + reload — precached old bundle serves first, normal
  PWA behavior). Screenshots: screengrab/lobby-*.png, setup-*.png,
  game-lobby-thumb.png, prod-smoke-phase10b.png.
Out of scope (still): rules/pacing changes (own phase when the playtest
verdict lands), board gallery/sharing, thumbnail image export/caching,
editor changes, third built-in board.

### Phase 11 — Board gallery & sharing  [DONE 2026-07-06, deployed]
Delivered (engine/store/EventLog untouched; tests stay 86):
- **Schema**: `boards` gains `publishedAt?: number` + `authorName?: string`
  and index `by_publishedAt`. No migration — absent = private, all
  pre-existing boards stay private.
- **convex/boards.ts**: `publish({boardId, authorName})` — creator-only,
  re-runs `validateBoard` on the SERVER copy (publish shares the saved
  board, not unsaved edits), trims byline (1–40 chars, same rule as board
  names), stamps `publishedAt`; re-publish updates the byline.
  `unpublish({boardId})` clears both fields. `gallery()` — signed-in only,
  newest-first via `by_publishedAt` range `gt 0` (undefined sorts below 0,
  so private boards never match), `.take(50)`, returns `{boardId, name,
  board, authorName, publishedAt, mine}`. `myBoards` + `get` now also
  return `publishedAt` (null when private).
- **games.createGame**: foreign `boardId` is accepted when the board is
  published (`createdBy === userId || publishedAt !== undefined`); same
  snapshot-at-create semantics, so unpublish/edit/delete never touches
  existing games (verified live).
- **`#/gallery` route** → `GalleryScreen` (new, in SignInGate): intro
  note, name input (useSavedName — a fresh guest landing here can still
  set a pilot name; button disabled while empty), `.gallery-grid` of
  cards (BoardThumb tilePx 10 / maxPx 200, name, W×H, "by <author>" or a
  "yours" chip, "New game on this board" → createGame + navigate, per-card
  busy).
- **EditorToolbar**: `PublishButton` next to Save (cloud boards only) —
  published state from the existing `boards.get` subscription (deduped),
  Publish/Unpublish toggle with SaveOnlineButton's busy/note treatment;
  byline = saved display name, falls back to "anonymous".
- **LobbyScreen**: fifth card "Board gallery — play boards other pilots
  published" → `#/gallery`; saved-board picker cards get a "published"
  badge via the new myBoards field.
- **index.css**: `.gallery-grid` auto-fill minmax(230px,1fr); `.gallery-
  card` centered lobby-card variant; `.published-chip`; **`.lobby-header
  .back-link {position: static}`** — `.back-link` is position:fixed and
  overlapped the gallery title on mobile until pinned into header flow.
- Verified: typecheck + 86 tests; Playwright on dev (port 5181 — 5173-80
  busy): A publishes Thumb Test (button flips to Unpublish, lobby badge
  shows, gallery says "yours"); fresh guest B (localStorage auth swap)
  sees "by Bob", creates a game on it (game lobby shows the snapshot
  thumb); A unpublishes → B's gallery empty but B's game still renders;
  signed-out #/gallery shows the guest gate; 375px + desktop 1280
  (gallery, editor toolbar wrap). Re-published on dev at the end. Deployed
  (Convex prod added by_publishedAt + CF Pages); prod smoke after SW
  reload: gallery card in lobby, empty gallery screen renders. Screenshots:
  screengrab/gallery-*.png, editor-375.png.
Out of scope (still): rules/pacing changes (own phase when the playtest
verdict lands), ratings/comments, search/pagination, board forking,
third built-in board, real auth.

### Phase 12 — Board forking from the gallery  [DONE 2026-07-06, deployed]
Delivered (client-only, zero Convex changes; tests stay 86):
- **GalleryScreen**: each card gets a quiet second button "Open a copy in
  the editor" under the primary — `loadDraft({...b.board, name:
  forkName(b.name)})` + `navigate('#/editor')`. `forkName` = `Copy of
  <name>` sliced to 40 (board-name cap). No confirm dialog: `loadDraft`
  commits into undo history, so one Ctrl+Z restores the prior draft
  (stated in the button `title`). Spread is shallow on purpose —
  `loadDraft` already `structuredClone`s.
- **Detachment is free**: arrival at `#/editor` (no boardId) means Save
  online inserts a NEW row (URL → `#/editor/<newId>`); original board
  never touched. No editor/Convex changes needed — verified, not modified.
- **CSS**: none — button reuses the default look inside `.gallery-card`.
- Verified: typecheck + 86 tests; Playwright on dev (5181): fork Thumb
  Test → editor shows valid "Copy of Thumb Test" at `#/editor`, undo
  restores prior draft ("Thumb Test"), redo + Save online mints new id
  (`npx convex data boards` confirms: copy row unpublished, original
  still published with updatedAt == creationTime i.e. untouched), lobby
  picker lists "Copy of Thumb Test" (no badge) beside "Thumb Test ·
  published"; 375px card layout (both buttons 44px, no h-overflow,
  screengrab/gallery-fork-375.png). Deployed (Convex prod no-op + CF
  Pages); prod smoke after SW update: published a throwaway "Prod Fork
  Smoke" board, forked it from the gallery ("Copy of Prod Fork Smoke",
  valid, `#/editor`), then unpublished — prod gallery back to empty.
Out of scope (still): rules/pacing changes (own phase when the playtest
verdict lands), forking from game screen/lobby picker, attribution chain,
ratings/search/pagination, third built-in board, real auth.

### Phase 13 — Fork attribution  [DONE 2026-07-07, deployed]
Delivered ("forked from X by Y" on gallery cards; tests 86 → 88):
- **Placement decision**: attribution lives on the boards TABLE ROW (like
  `authorName`), NOT in `BoardDef` — engine type stays pure, game
  snapshots don't carry presentation metadata. Client-side it rides
  beside the draft in editorStore + the localStorage wrapper.
- **Schema**: `boards` gains `forkedFrom?: {name, authorName}` — a
  name+byline SNAPSHOT of the immediate parent (no boardId link;
  originals can unpublish/vanish, card text survives). No migration.
- **convex/boards.ts**: `save` takes optional `forkedFrom` (same shape),
  honored on the INSERT path only — a fork is always a new row; patch
  ignores it, so renaming/editing your fork keeps attribution. `gallery`
  returns `forkedFrom ?? null` per row.
- **editorStore**: new `forkedFrom: ForkAttribution | null` (exported
  interface), NOT in undo history (attribution follows the draft's
  origin, not the paint trail). `loadDraft(board, forkedFrom?)` —
  defaulting null, so Import JSON + cloud-board hydration clear it for
  free; `reset()` clears (set BEFORE commit so the persist sees it).
  localStorage wrapper is now `{board, forkedFrom}` (missing key parses
  null — old drafts fine); all `persistDraft` callers pass
  `get().forkedFrom`.
- **SaveOnlineButton**: passes `forkedFrom` only when inserting
  (`boardId ? undefined : forkedFrom ?? undefined`).
- **GalleryScreen**: fork onClick passes `{name: b.name, authorName:
  b.authorName || 'anonymous'}` as loadDraft's 2nd arg; card shows a
  second `.lobby-card-note` line "forked from <name> by <author>" when
  set. CSS: none.
- Verified: typecheck + 88 tests (2 new: loadDraft-with-attribution sets,
  plain loadDraft/reset clear); Playwright on dev (5175): fork Thumb Test
  → Save online → row has `forkedFrom {Bob, Thumb Test}` (convex data) →
  publish copy → card reads "forked from Thumb Test by Bob"; localStorage
  wrapper carries it across the flow; Import JSON → wrapper null → Save
  online → row's forkedFrom column empty; 375px card clean
  (screengrab/gallery-fork-375.png). Cleanup: copy unpublished. Deployed
  (Convex prod additive schema + CF Pages); prod smoke after SW update:
  gallery renders on new bundle (empty as Phase 12 left it).
Out of scope (still): rules/pacing changes (own phase when the verdict
lands), forkedFrom.boardId live link, multi-level chains, ratings/search/
pagination, third built-in board, real auth.

### Phase 14 — Third built-in board: The Gauntlet  [DONE 2026-07-07, deployed]
Delivered (12×12, walls + lasers — route choice under fire; one-file
change, tests stay 88):
- **src/engine/boards.ts only**: `theGauntlet()` factory in house style +
  `'the-gauntlet'` registry entry (third → third card in both pickers).
  No picker/Convex/CSS changes — registry-driven as designed.
- **Three routes north** from spawns (2,4,7,9 @ y=11): west safe lane
  (x=0–1, walls E of (1,3..6) commit it mid-board, hazard-free, long);
  center gauntlet (two-lane walled corridor x=5–6, y=2–9, one laser per
  lane firing along it); east express belts (9,9)→(9,7)→(10,7)→(10,4)
  skirting a pit column at (9,4..6).
- **Mouth-cap trick**: wall S of (5,9) + wall N of (6,2) seal one end of
  each lane — the SAME wall stops that lane's beam at the corridor
  (validate: zero spawn-in-laser warnings, no fire on the approach) and
  forces a mid-corridor lane switch (enter via lane 6, exit via lane 5
  onto CP1). Rescue gears sit in the sealed-mouth pockets: CW (5,10),
  CCW (6,1).
- **Checkpoints spread the legs**: CP1 (5,1) at the corridor exit (leg 1
  favors the gauntlet), CP2 (1,10) southwest (leg 2 favors the safe
  lane), CP3 (10,2) at the belt exit (leg 3 favors the belts). No route
  wins all three.
- **Damage economics**: 1 dmg per register-end inside a lane's beam; a
  careless full-turn run ≈5 dmg, a tight transit ≈3.
- Verified: typecheck + 88 tests (registry validate test auto-covers:
  name match, zero errors AND warnings); engine test-drive via throwaway
  vite-node script (deleted) — 15/15 checks: corridor run 5 dmg ending
  (5,2), beams confined (idle outside both mouths = 0 dmg), sealed-mouth
  wall block + gear CW rescue, belt ride (9,10)→(10,3) past pits
  unharmed, pit death → respawn at archive w/ 2 lives, lane-5 exit tags
  CP1 @ 1 dmg. Playwright on dev (5175): hot-seat 2P game on the new
  card, full turn 1 replay — wall block + gear spin animate, end
  positions match engine exactly (Ann (2,5)E safe lane, Bob (7,10)E);
  pickers show 3 built-in thumbnail cards at 1280 + 375 (scroll-snap;
  screengrab/gauntlet-*.png); console clean. Deployed (Convex prod
  redeploy no-op + CF Pages); prod smoke on new bundle: 3 boards in
  picker, hot-seat game on The Gauntlet renders + deals.
Out of scope (still): rules/pacing changes (own phase when the verdict
lands — including re-tuning this board), fourth board, gallery
search/pagination, real auth, new sprites.

### Phase 15 — Playtest readiness: telemetry, README, docs  [DONE 2026-07-07, deployed]
Delivered (tests 88 → 92):
- **Convex sink**: `telemetry` table (kind/message/data?/context/userId?,
  no indexes — `_creationTime` orders it). `convex/telemetry.ts`:
  `log` = PUBLIC mutation, auth optional (works signed-out), truncates
  message @2k chars, drops `data` over ~8KB JSON (and non-serializable
  data), whole handler try/caught — never throws at a client that's
  already broken. `recent` = internalQuery {limit?} newest-first
  (capped 200) — `npx convex run telemetry:recent [--prod]` or dashboard.
- **Client**: `src/services/telemetry.ts` — `logTelemetry(kind, msg,
  data?)`, kind ∈ error|unhandledrejection|react-error|note. Every call:
  localStorage ring buffer `dd-telemetry` (last 100, offline-safe) +
  fire-and-forget `convex.mutation` (.catch-swallowed; convex null =
  buffer only). 5s kind+message dedupe (error loops), whole body
  try/caught. Context = {href, ua, sessionId (random per load), ts}.
  `installGlobalTelemetry()` (main.tsx) wires window error +
  unhandledrejection and `window.ddTelemetry = {dump, note}`.
- **App.tsx**: RouteBoundary gains componentDidCatch → 'react-error'
  (componentStack in data). App now renders `<Screen/> + <BugButton/>` —
  fixed 🐞 bottom-LEFT (faded, 36px, z-9; dock's controls are right/in
  flow), window.prompt → 'note'; href in context carries route/gameId.
- **README.md** (new, root): play-now link, two-terminal quick start,
  playtest firing instructions (prod multiplayer + hot-seat solo),
  test-everything checklist (screens + mechanics per board), where logs
  land. CLAUDE.md: telemetry arch note (never import into engine) + README
  pointer.
- Engine untouched — telemetry is all service/backend layer.
- Verified: typecheck + 92 tests (4 new: ring trim @100, 5s dedupe incl.
  kind-splits, no-throw sans localStorage, corrupted-buffer survival);
  local dev + anonymous convex: console-thrown error, rejected promise,
  and 🐞 note all landed in `telemetry:recent` AND ddTelemetry.dump().
  Deployed (Convex prod additive schema + CF Pages); prod smoke: new
  bundle, 🐞 note read back via `telemetry:recent --prod`.
Out of scope: dashboards/aggregation, log rotation/pruning (volume is one
playtester), auth-gating reads beyond internalQuery.

### Phase 16 — Node 22 + Vite 7 upgrade  [DONE 2026-07-13]
Delivered (infra only, zero source changes):
- **Node 20.18.0 → 22.23.1** via nvm-windows (`nvm install 22` +
  `nvm use 22.23.1`). Gotcha fixed along the way: the *User*-scope
  `NVM_HOME`/`NVM_SYMLINK` env vars had literal quotes embedded
  (shadowing correct Machine-scope values), which broke every nvm
  command — rewrote them clean at User scope. Note: `nvm use`
  recreates the `C:\nvm4w\nodejs` symlink asynchronously (elevation
  helper), so immediate existence checks can race.
- **package.json bumps**: vite ^6.3.5 → ^7.3.6, vitest ^3.2.4 →
  ^4.1.10, @vitejs/plugin-react ^4.6.0 → ^5.2.0. vite-plugin-pwa
  ^1.3.0 already allows Vite 7 — untouched. (Vite 8 is out but
  plugin-react 6 requires it exclusively; deliberate stop at 7.)
- Verified: typecheck; 92 tests green on Vitest 4 (no API breaks hit);
  `npm run build` incl. PWA injectManifest; `npm run preview` +
  Playwright smoke — app boots, SW activates, new bundle served after
  SW update, only expected local-Convex connection-refused errors;
  `npm run dev` boots (Vite 7.3.6, deps re-optimized); convex CLI
  1.42.1 runs on Node 22.
- Rollback if needed: `nvm use 20.18.0` + revert package.json/lock.

### Phase 17 — Editor mobile layout pass  [DONE 2026-07-13]
Delivered (tests 92 → 95; engine + Board renderer untouched; Opus
subagent implemented, orchestrator fixed 4 review findings):
- **index.css** (new editor section in the ≤700px block): `.editor-layout`
  → column; `.tool-palette` → horizontal scroll-snap row (mirrors
  `.board-picker`); `.validation-panel` full-width below; toolbar buttons
  + board-name + size-stepper ≥44px; `.tool-options` stays VISIBLE as a
  compact row pinned `position: sticky; left: 0; order: -1` at the
  palette row's start (selecting conveyor/gear pops options into view;
  they'd otherwise render off-screen at the row's end). Edge strips
  thicken to `max(10px, 0.3*var(--tile))` on the thickness axis only —
  cross axis keeps the desktop insets so the full edge spans. `.editor-
  board-wrap` centers via `align-self: center` (flex-centering the Board
  INSIDE the wrap skews it off the absolutely-positioned hit layer —
  caught at 375px as a 1.5px paint offset). `touch-action: none` moved
  to the BASE `.editor-hit-layer` rule (tablets > 700px paint too).
- **EditorBoard.tsx**: per-cell mouse handlers REPLACED by layer-level
  pointer events — pointerdown (setPointerCapture + beginStroke + paint),
  pointermove (hit-test `document.elementFromPoint` → `data-x`/`data-y`
  on `.hit-cell`, dedupe repeat cells, `buttons === 1` guard), pointerup/
  cancel (release + endStroke). Capture means per-cell pointerenter never
  fires on touch; elementFromPoint is the mechanism. Guard: a second
  concurrent touch can't split the stroke. Right-click erase kept via
  per-cell contextmenu; `.hit-edge` handlers converted to pointerdown
  (tap works on touch; toggles double as edge-erase there). The old
  window-mouseup stroke closer is gone — capture guarantees pointerup.
- **editorStore.ts + ToolPalette.tsx**: `'eraser'` ToolId (paintTile
  delegates to eraseTile; tiles only) + palette entry w/ X-box icon.
- Review findings fixed after the subagent: (1) per-cell mousedown left
  in place → double beginStroke = every desktop click cost two undo
  steps; (2) `.hit-cell` given min 44px → hit grid misaligned off 33px
  tiles; (3) blanket `.hit-edge` w+h override → N/S edges shrank to
  10px squares; (4) `.tool-options` display:none → conveyor direction /
  gear rotation unreachable on mobile.
- Verified: typecheck + 95 tests; Playwright dev-server. Desktop
  1280×800: 4-cell drag = ONE undo step (undo disables after — exactly
  one history entry), redo, right-click erase, eraser click, wall edge
  click. Mobile 375×667 (CDP touch events): stacks toolbar→palette→
  board→validation, no horizontal overflow, palette scroll-snap row,
  all controls 44px, hit layer pixel-aligned to tiles, touch drag =
  one undo step, eraser tap, wall tap + tap-again-toggles-off, laser
  tap (edge thickness 10px @ 33px tiles), conveyor dir + gear CW/CCW
  tappable 44px and sticky-pinned. screengrab/editor-mobile-375.png,
  editor-desktop-1280.png.
Out of scope: editor keyboard shortcuts on mobile, multi-touch
gestures (pinch zoom), rules/pacing (standing gate).

## (was NEXT after §17; still the standing gate) THE fun-playtest — Todd plays on prod; crashes now self-report and 🐞 notes capture feel issues, so afterwards mine `npx convex run telemetry:recent --prod` + verdict → rules/pacing tweaks become their own phase (informing a Gauntlet re-tune). Backlog: Resend/Google auth (waiting on creds), gallery search/pagination (when board count warrants).

## Phase 18 — Sound effects (2026-07-13) DONE
- Kenney CC0 clips (Sci-Fi/Interface/Impact/Casino/Digital/Jingles packs),
  18 clips → mono MP3 via ffmpeg (winget Gyan.FFmpeg — choco's 2020 build
  has no libmp3lame), 95KB total in public/sounds/ + CREDITS.md. mp3 added
  to PWA injectManifest glob (plus svg/png — icons now precache too).
- src/services/audio.ts (Opus subagent + review fixes): zero-dep Web
  Audio, lazy AudioContext, pointerdown unlock, Promise.allSettled
  loadAll, fail-silent everywhere (telemetry philosophy). EVENT_SOUND
  exhaustive map w/ `satisfies` (turn/register markers explicitly null);
  pushed→bump, express→pitch 1.3. Mute persisted in dd-muted.
- Review fixes on subagent work: loadAll iterated EVENT_SOUND values →
  bump/card-deal/click never loaded; added SOUND_NAMES manifest + test
  pinning it to public/sounds/*.mp3 on disk.
- Wiring: ReplayPlayer effect on cursor (skip-to-end = final event only),
  card-deal on ProgrammingView mount, click on submit, 🔊/🔇 in lobby
  header + replay controls (44px).
- **Todd's ear-check verdict: clips too annoying — sounds now DEFAULT
  MUTED (opt-in via toggle; dd-muted '0' = opted in). Better clips are
  a future phase; infra + curation method stay.**
- Verified: 107 tests, typecheck, Playwright (18× fetch 200, ctx running
  after gesture, mute persists reload, fresh session = 🔇, no console
  errors, replay clean).

## Phase 19 — Vortex Arena board (2026-07-13) DONE
- boards.ts only: vortexArena() + 'vortex-arena' registry entry (4th
  picker card everywhere, registry-driven). 11×11: clockwise EXPRESS
  whirlpool ring (16 cells) around a pit-guarded core (4 corner pits,
  4 gear gates on cardinals, CP3 center), express on-ramps at N/E/S/W,
  CCW rescue gears off NE/SW ring corners, CP1 (9,2) / CP2 (1,8),
  lasers sweep rows 1+9 (tax outer lanes AND N/S ramp mouths; spawns
  row 10 stay out of beams).
- Drive-script findings (throwaway vitest file, deleted): ring carries
  2 cells/register (10/turn, closed loop); rest cells align with gate
  cells every other register; gate-gear re-spins each register → single
  rotates can never face N/S again (flanks are pits) — Move2 shoots
  through the gate un-spun; u-turn is the only trap escape. Kept as the
  board's signature risk (telegraphed, self-inflicted, escapable).
- Verified: validateBoard 0 errors/0 warnings, 107 tests, Playwright
  hot-seat full turn on the board (ramp→ring ride replayed clean),
  pickers at 1280+375. screengrab/vortex-*.png, lobby-4boards-*.png.

## Phase 20 — Hand-rolled visual polish (2026-07-13) DONE
- sprites.tsx + Board.tsx + index.css only (Opus subagent + 2 review
  fixes). Conveyor chevrons scroll via CSS keyframes on an inner <g>
  (period = chevron spacing, wrap chevrons for seamless loop; 1.8s
  normal / 0.9s express); per-seat silhouettes (dome/square/wedge/
  antennae + bright cyan nose light, readable at 30px, seat prop from
  Board); laser beam = gradient hot core + layered glow + pulse;
  damage event → .damage-flash (tint on .robot, shake on inner
  .sprite). ALL motion inside prefers-reduced-motion: no-preference.
- Review fixes on subagent work: (1) scroll keyframes translated +Y —
  belts ran BACKWARDS; flipped to -Y (local N = travel dir). (2)
  damage-shake animated transform on .robot, which would override the
  inline board-position translate (robot teleports to origin mid-
  shake); moved shake to the inner .sprite.
- Verified: typecheck + 99 tests (drive script deleted); Playwright —
  4-robot Spin Cycle (silhouettes distinct, belts 1.8s/0.9s, -Y flow),
  emulated reduced-motion → animationName none, Proving Grounds replay
  laser (beam-flash+beam-pulse, gradient, layered shadows) and damage
  (tint+shake, position transform intact), 375 view, zero console
  errors. screengrab/polish-*.png. README checklist updated (4 boards,
  opt-in sound toggle, animation notes, Vortex Arena mechanics).

## Phase 21 — In-app "How to Play" screen (2026-07-14) DONE
- New `#/rules` route (route.ts union + parseHash + comment) rendered in
  App.tsx ABOVE the `if (!convex)` fallback — static screen, works with
  no backend/sign-in, so join-link first-timers can read it pre-auth.
- src/components/rules/RulesScreen.tsx: player-facing rewrite of
  docs/game_mechanics_md.md (goal / a turn / per-register order / card
  table (7 types, 84 deck) / board-element legend reusing the REAL
  sprites from sprites.tsx in 52px tile boxes (pit + conveyor rows get
  the matching tile backgrounds; robot wrapped in a .rules-robot-body
  with --player-0 like Board's .robot-body) / damage / lives-respawn /
  one-liner per built-in board). Back = history.back() when there's
  history, else `#/` — join-link visitors return to the invite intact.
- Entry links: lobby 6th card "How to play", JoinScreen quiet-link
  "First time? How to play" under the join button, hot-seat SetupScreen
  inline link in the subtitle (.setup-rules-link kills primary-link's
  margin-top).
- New src/services/route.test.ts — parseHash all routes + home fallback
  (incl. argless #/game, #/join). Tests 99 → 101.
- Board audit (no code): 4 built-ins each teach a distinct mechanic
  (starter / belts / lasers / whirlpool) — decent playtest spread,
  no board work needed.
- Verified: typecheck, 101 tests, build, Playwright smoke (lobby card,
  join-link → rules → back-to-invite, hot-seat link, direct #/rules),
  deployed + prod smoke.

## Backlog: multi-board composition (future phase, RoboRally-style)
Larger play spaces by tiling boards edge-to-edge (docks board + factory
floor, like the tabletop). Engine already handles arbitrary W×H
(`emptyBoard(name, w, h)`) — composition is purely a BoardDef concern:
stitch two+ BoardDefs by offsetting coordinates (tiles, walls, lasers,
checkpoints renumbered, spawns from the docks board) into one composed
def. Touches: a compose helper in boards.ts (or editor), board-picker
thumbnails (taller aspect), editor support if user-composable, and the
mobile board-scale pass (20×10+ needs smaller --tile / pan). No engine
changes expected.

## Phase 22 — Pit Archipelago board (2026-07-17) DONE
- boards.ts: pitArchipelago() + 'pit-archipelago' registry entry (5th
  picker card, registry-driven). 12×11: south mainland (spawns row 10),
  full-width pit channels at y=8 and y=4, middle band split by a pit
  gate column at x=6 → 4 land masses, one checkpoint each side of every
  channel (CP1 west-middle (1,5), CP2 east-middle (10,6), CP3 north
  (5,1)) so every leg forces a crossing.
- Crossings per channel: a 1-wide floor causeway (free, exact programs;
  a wrong column or a shore shove = a life) and a 2-belt normal-belt
  bridge (hands-free). South bridge swept by a laser up its column
  (wall N of (8,7) caps the beam) ≈2 damage per crossing; row-6 laser
  sweeps the west island into the gate, wall E of (7,6) shields the
  east island. CW/CCW gears on both causeway landings spin arrivals.
- PLAN PREMISE CORRECTED: Move 2 does NOT jump 1-wide pits (per-cell
  movement, pits.test.ts) — "jump the gap" replaced by narrow-causeway
  risk. Vortex's "gate trick" is passable cells BETWEEN pits, not jumps.
- Verified: validateBoard 0 errors/0 warnings (registry test), 8-case
  throwaway drive test (deleted): causeway Move2 + gear spin, misaligned
  step dies, both bridges ferry idle robots safely, bridge tax ≥2,
  gate beam 5 dmg/turn vs shielded east island 0, shore shove kills,
  CP1 claims. 109 tests, typecheck, Playwright hot-seat full turn on
  the board at 1280 (Ada crossed to the causeway in replay) + 375
  (fits, legible), rules screen lists 5 boards, zero console errors.
  screengrab/pit-archipelago-*.png. README + RulesScreen updated.
- NOTE: local port 5173 is another app (Leyline Tycoon) — drone_derby
  dev server lands on 5175 when both are up.

## Phase 23 — Ghost preview while programming (2026-07-17) DONE
- Todd's request, jumped the queue: placing a card plays a translucent
  ghost of your robot through the turn-so-far — deterministic board
  effects only (belts express-first, gears, walls, pits, board lasers,
  checkpoints). "Strategy, not memory."
- src/engine/preview.ts: previewProgram(state, player, program) →
  EventLog. Solo state (robots: [me] — rivals EXCLUDED, not frozen;
  ghost passes through them), executeTurn seed 0, events truncated
  after the last filled register (locked registers count as filled;
  empty middles idle but board effects still pulse). Death events kept,
  respawn cut — the ghost stays gone; the death IS the warning.
- BUG CAUGHT LIVE: online clients get redacted state (games.ts:103,
  decks: {}, only own hand) → solo sim crashed in cleanUpCards.
  Fix: substitute empty deck/hand; regression test pins it.
- Board.tsx: ghost?: {robot, seat} prop → .robot.ghost element
  (RobotSprite + seat color, opacity .45, own useGhostAngle since it
  shares its player id with the real robot). ProgrammingView: useMemo
  preview log + 220ms setTimeout stepper, restarts on any placement
  change, rests at final pose; reduced-motion jumps straight there.
  Ghost visual = visualAt(initialVisual(game), events, cursor).
- Verified: typecheck, 109 tests (8 new in preview.test.ts: determinism
  /no-mutation, rivals excluded, truncation, empty-program, locked-past-
  unfilled, pit death no respawn, redacted state, belt→belt→gear order).
  Playwright dev 1280+375: hot-seat Proving Grounds — ghost settles on
  engine-predicted cells ((1,9)→(1,8), +Move2 →(1,6)), re-runs from
  start on card removal, vanishes when last card removed, Turn Right
  rotates body 90°; online 2-context game (guest sign-in via isolated
  browser context) ghost works post-fix; reduced-motion emulation jumps
  to pose; zero console errors. README checklist + RulesScreen updated.

## Phase 24 — Multi-board composition (2026-07-17) DONE
- src/engine/compose.ts: composeBoards(parts, name) stacks BoardDefs
  top-to-bottom (parts[0] = northmost). Spawns kept ONLY from the
  bottommost part that has any (upper spawns silently stripped);
  checkpoints renumber bottom part first then upward, preserving each
  part's internal order; narrower parts centered + floor-padded;
  walls/lasers offset with their part. Result gated by validateBoard —
  throws its first error (size cap, spawn count, 0 checkpoints).
  MAX_BOARD_SIZE 16→24 (validate.ts; error string interpolates).
- boards.ts: dockyard() — 12×7 staging yard (4 spawns, bay dividers +
  baffle walls, no checkpoints; not standalone-playable by design) +
  6th registry entry 'grand-circuit' = composeBoards([spinCycle(),
  dockyard()]) → 12×17. Exercises spawn-stripping and the old 16 cap.
- Editor: appendBoard(source) in editorStore composes [source, draft]
  (draft stays the bottom part → keeps its docks; one undo step; alert
  on throw). AppendBoardModal (built-in cards + import JSON with shape
  check only — docks-less parts are legal sources), "⬆ Append" button
  by the size steppers. Store resize clamp follows MAX 24.
- Mobile: tileFit gains a 24px floor (clamp(24px, fits, 52px)); Board
  wraps its grid in .board-viewport (fit-content, max = tileFit's
  viewport terms + 6px for .board borders, overflow auto) — floored
  boards pan instead of shrinking. EditorBoard adds its OWN outer
  .board-viewport around wrap+hit-layer (they scroll together) and CSS
  neutralizes the nested Board one (overflow visible) so the absolute
  hit layer never drifts. BoardThumb tile formula gains the /height
  term so tall thumbs fit their picker card.
- Verified: typecheck; 117 tests (8 new in compose.test.ts: offsets,
  centering pad, spawn ownership both directions, bottom-up renumber,
  size-cap + no-cp/no-spawn throws, validates clean, determinism/no
  input mutation; validate.test big-board case 17→25, store resize
  clamp 16→24). Playwright dev 1280+375: 6 picker cards, GC thumb
  98×138 in card; hot-seat GC renders 12×17, ghost preview correct
  from dock spawn (2,16)→(2,14); 375×550 floors tiles at 24px and
  pans (sh 414 > ch 364), no body x-scroll; editor append Spin Cycle
  over fresh 10×10 → 12×20 with belts/walls/lasers/spawns placed,
  undo reverts in one step, append past 24 alerts ("got 12×37") and
  leaves draft intact, hit layer aligned while panned, touch-action
  none. Zero console errors. README + RulesScreen list 6 boards.
- Dev server note: 5173/5174/5175 busy → landed on 5176 this session.

## Phase 25 — Softer SFX pack (2026-07-17) DONE (pending ear-check)
- Re-curated 13 of 18 clips in public/sounds/ (same filenames → zero
  test churn): harsh Impact/Sci-Fi picks swapped for soft Kenney
  Interface Sounds / UI Audio / Digital Audio clips (move=rollover1,
  bump=drop_002, thud=minimize_006, servo=scratch_001, belt=scroll_002,
  gear=toggle_002, laser=zap1, hit=lowRandom, lock=lowDown,
  explosion=spaceTrash1, eliminated=zapThreeToneDown; life-lost/
  respawn/checkpoint/click re-sourced same clips; card-flip/card-deal/
  fanfare kept, reprocessed). Full table in public/sounds/CREDITS.md.
- Treatment per clip (winget Gyan ffmpeg 8.1.2): trim to budget
  (≤0.6s per-register, ≤1.5s one-offs) → 10ms fade-in / 60ms fade-out
  (areverse trick) → measure LUFS → gain to −23 LUFS integrated with
  true peak capped at −2 dBFS → mono 44.1k MP3 q5. Verified: all 18
  land −23±0.1 LUFS or quieter (peak-capped shorties), ~125KB total.
  Pipeline script: scratchpad process-sounds.sh (session-local).
- audio.ts: CLIP_VOLUMES now lists all 18 explicitly (normalized base
  → values ARE the mix): per-register 0.35–0.55, notifications
  0.6–0.7, checkpoint 0.8, fanfare 0.9; card-flip/move/click boosted
  (peak-capped quiet). No master-gain change (mute still gain 0/1).
- Verified: 117 tests + typecheck; Playwright on dev 5176 — 18 mp3s
  fetch 200 AND decodeAudioData-clean, ctx running after gesture,
  full 2p hot-seat replay with 🔊 on, unmute persists reload, key
  removed → fresh 🔇 default, zero console errors.
- Sounds remain OPT-IN (default muted). Gate: Todd's ear-check on a
  replay — if vetoed, re-curate; shipping meanwhile is harmless.

## ⚠️ NEXT: Docs "invite friends" walkthrough — a short README (or
in-app rules screen?) section walking a friend from invite link →
name → first turn; goal is unblocking THE STANDING GATE (Todd's
friends playtest → mine telemetry). Auth creds still pending. Also
pending: Todd's ear-check verdict on the phase-25 sound set.
